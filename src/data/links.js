const startName = '/ffe-38-diplom';

const links = {
   main: startName,
   tickets: `${startName}/tickets`,
   seats: `${startName}/seats`,
   passengers: `${startName}/passengers`,
   paying: `${startName}/paying`,
   verify: `${startName}/verify`,
   successOrder: `${startName}/successOrder`,
};

export default links;